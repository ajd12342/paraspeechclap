# Classification: clarity attribute
# Usage: python scripts/evaluate_classification.py --config-name eval/classification/clarity checkpoint_path=/path/to/ckpt

defaults:
  - eval/classification/base
  - _self_

data:
  dataset_name: ajd12342/paraspeechclap-eval-intrinsic
  split: classification_clarity

candidate_styles: ["crisp", "slurred", "lisp", "stammering"]
