# Classification: texture attribute
# Usage: python scripts/evaluate_classification.py --config-name eval/classification/texture checkpoint_path=/path/to/ckpt

defaults:
  - eval/classification/base
  - _self_

data:
  dataset_name: ajd12342/paraspeechclap-eval-intrinsic
  split: classification_texture

candidate_styles: ["silky", "husky", "raspy", "guttural", "vocal-fry"]
