# Classification: volume attribute
# Usage: python scripts/evaluate_classification.py --config-name eval/classification/volume checkpoint_path=/path/to/ckpt

defaults:
  - eval/classification/base
  - _self_

data:
  dataset_name: ajd12342/paraspeechclap-eval-intrinsic
  split: classification_volume

candidate_styles: ["booming", "authoritative", "loud", "hushed", "soft"]
